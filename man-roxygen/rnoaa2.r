#' @param datasetid (optional) Accepts a single valid dataset id. Data returned will be from the 
#'    dataset specified, see datasets()
#' @param startdate (optional) Accepts valid ISO formated date (yyyy-mm-dd). Data returned will have 
#'    data after the specified date. Paramater can be use independently of enddate
#' @param enddate (optional) Accepts valid ISO formated date (yyyy-mm-dd). Data returned will have data 
#'    before the specified date. Paramater can be use independently of startdate